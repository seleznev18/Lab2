n=int(input('Введите n:'))
a=0
for i in range(n):
    n = [x for x in range(1, 33)]
    while len(n) > 0:
        middle = len(n) // 2
        if n[middle] == num:
            break
        elif n[middle] < num:
            n = n[middle+1:]
        else:
            n = n[:middle]
        a+=1
print("Макс. количество итераций алгоритма 4 = " + str(a))
